# Public WhatsApp support number (digits only, no `+`, country code first).
# Used by /terms, /privacy, and any "Contact us" link to build https://wa.me/<digits>.
# Default fallback in code is a clearly-broken sentinel so unset envs are visible.
VITE_SUPPORT_WHATSAPP=220***09
